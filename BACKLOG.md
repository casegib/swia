# SWIA System Backlog

Working list of planned improvements. Items graduate from here into implementation passes.

## Next up

- Smoke-test the guides in Foundry: the Compendiums tab shows "SWIA Guides" inside the Imperial Assault folder with two journals; open the Player Guide, confirm 12 pages in the sidebar with the tables rendering in Quick reference; as a player, confirm both journals are readable.
- Smoke-test cross-figure declared effects in Foundry (packs rebuilt): give Gideon Called Shot and attack with Fenn — the attacker panel lists "Gideon Argus › Called Shot (+1 surge)"; as Gideon's player (not controlling Fenn) click it and confirm Gideon's card exhausts and Fenn's Surge row reads 1; Undo readies Gideon's card. Defend with Fenn while Ko-Tun holds Dig In: "Ko-Tun Feralo › Dig In" on the defender panel, declaring takes 1 strain from Ko-Tun and exhausts her card, Block row +2. Put Ringleader on Vader and attack with a Stormtrooper group: "Darth Vader › Ringleader" offers +1 damage / +1 accuracy at no cost. Solo: as Fenn's player with Gideon not yours, Called Shot is absent from the dialog; as GM it shows and exhausts Gideon's card on Roll. Open Called Shot's sheet: the For field reads "Own or a friend's roll".
- Smoke-test the item QoL batch in Foundry: drag a weapon row above another in the Weapons column (dashed outline on the target, order sticks after F5, and a player can do it on their own sheet); give a modded weapon from Biv to Fenn via the hand icon (mods follow, chat line posts) and give armor (arrives unequipped); as a player give to another player's hero with the GM online and watch it relay; add an exhaust ability on a weapon sheet, save, expand the weapon on the hero sheet and click Use (chat card + exhausted, button disabled after); turn on *Class cards cost XP*, give Biv 1 XP, drop a 1-XP card (XP → 0, notice) then a 2-XP card as a player (refused) and as GM (override prompt); hover an item in the Imperial portal to confirm the preview still shows.
- Smoke-test the imperial pass in Foundry (packs rebuilt): drag Sharpshooters from the Class Cards (Imperial) pack into the world (it appears in the Imperial portal's class card list) and attack with any villain — the Accuracy row starts at 1 with "Sharpshooters (+1 accuracy)" in the notes; drag Shock Troopers in and confirm a Class Cards button on the villain's attacker panel, declare it, and check the world card flips to exhausted in the portal, Undo readies it; put threat at 0 in the Campaign Tracker, drag Versatile Attack in, and confirm its button is disabled with the threat tooltip, then set threat to 2, declare, and watch the tracker drop to 1 (Cancel restores 2). Open a Stormtrooper villain sheet: an Attachments panel with the drop hint; drag Reactive Armor from the pack onto the sheet — it lists with a +2 Health chip, the health max rises by 2 with current following; drag Combat Veterans on and defend with the group (Block row seeds 1) and attack (Accuracy 2, Damage 1); drag Cloaking Device on and check the defense pool shows an extra white die. Drop a non-attachment card (Sharpshooters) onto a group and confirm the dashed row with the Class-wide tag. Open an imperial card sheet: class, XP and the Attachment toggle in edit mode, and the Class-wide / Attachment chip in view mode.
- Smoke-test class-card stage 3 in Foundry (packs rebuilt): give Fenn Trench Fighter and Take Cover; start a combat as Fenn and confirm the attacker panel shows a Class Cards block with Trench Fighter (+2 ⊠, Exhaust, "targeting a figure within 3 spaces"); click it — the card flips to exhausted on the sheet, the Damage row reads 2 with "Cards 2" in the tooltip, and an Undo appears; Undo readies the card and zeroes the row; declare again, roll, apply, and check the result card's damage; Cancel a fresh combat with a declared card and confirm it readies. Defend with Fenn: Take Cover adds a white die to the pool and takes 1 strain, Undo refunds both. Give Onar Hold Still and attack: both options show, declaring one disables the other, the defender's Block row drops by 1. Give an imperial figure Shadow Armor, attack it, roll the attack, then declare Shadow Armor from the defender panel and watch the attacker's Damage total drop by 1. Solo: Diala with Force Adept on a Strength test shows the card, toggling adds a blue die, rolling exhausts the card. Verena with Student of Battle: declare, roll, and confirm the three surge buttons appear labelled with the card's name. Open Trench Fighter's sheet in edit mode, change the note, save, and confirm the button text follows; add a declared effect to a plain class card by hand and see it in the dialog.
- Smoke-test class-card stage 2 in Foundry: load as GM (watch the console for the armor-modifier migration line; existing armor keeps its Health/Block/Evade and the armor sheet shows them as chips); open a hero, drag Art of Movement from the Class Cards pack and confirm Speed shows +1 with a hover breakdown; drag Rebel Elite and confirm Health max and Endurance max rise by 3 / 1 with current values following (damage taken unchanged); delete the card and watch them drop back; edit mode shows the printed base with a "+N" note and saving does not bake the bonus in; wound the hero and check the wounded side also carries the bonus; drag Bank Shot to a hero, start a combat and confirm the attacker Accuracy row starts at 1 with "Bank Shot (+1 Accuracy)" in the notes and the tooltip column reads Gear; solo-attack and check the card's accuracy total and note; give a hero Mon Cala Special Forces and defend — Evade row seeds +1 beside armor; open a class card in edit mode and set a passive by hand, save, confirm the chip and the stat change; open an armor in edit mode and confirm the modifier editor shows its Health/Block/Evade in the new grid.
- Smoke-test class-card stage 1 in Foundry: open a kitted hero and check Repeating Blaster / Trophy Armor / Vibrobayonet carry a Starter / N XP badge in their own columns and do not appear under Class Cards; feat cards show XP badges and sort by XP; open a class-deck weapon and armor and check the "Biv Bodhrik · 3 XP" chip; set heroClass on a plain weapon in edit mode, save, and watch the badge appear.
- Table-test the exhaust/surge mechanics and inventory management in a real session

## Planned

**Sheet layout pass 2 — plan in `docs/LAYOUT_PASS_2.md` (glyph swap → compact rows → two-column grid only if still needed; tabs rejected to keep the single-glance overview; one release)**
- Two-column inventory grid (Weapons + Armor wide on the left, Class Cards + Accessories on the right) — fallback if compact rows leave a kitted hero tall.

**Documentation**
- Screenshots for the guides: one per major surface, captured after each polish pass, kept in `docs/img/` and referenced from the markdown (the journal builder would need an image-page or inline `<img>` path for them).
- Keep the guides current: every pass that changes a player-facing surface edits `PLAYER_GUIDE.md` / `GM_GUIDE.md` in the same commit; `npm run build:packs` republishes the journals.

**Power-token follow-ups**
- Smoke-test the map badge in Foundry: grant a Block token from the portal and confirm the stock status icon disappears and the badge (icon + count) appears bottom-left of the map token; check an unlinked token too.
- Any in-flight combat saved before this build is dropped at load (its state shape predates the declared-bonus layers).
- Tokens declared in the solo roll dialog have no post-roll undo (same as the combat window, where the declaration window closes at the roll). If that bites at the table, stash the deleted effect data on the card the way the combat window does and add an Undo.

**Armor follow-ups**
- Conditions still carry their own record shape in `conditions.js` (they also discard, so they never merged into `modifierField`). If a third consumer of the shape appears, fold `normalizeCondition` onto `modifierField` + a discard block.
- Smoke-test in Foundry: duplicate a damaged hero wearing +N armor and confirm the copy keeps the same damage (Foundry should not fire `createItem` for items carried in `Actor.create` data; if it does, the copy heals by N).
- Conditional armor text ("+1 Block against Ranged") is still a manual stepper bump in the combat window; a typed "vs melee/ranged" field could auto-apply it.

**Class cards (see `docs/CLASS_CARD_SURVEY.md` — full census of all 272 cards, and `docs/class-cards.json` for the transcriptions + shape/model per card)**
- Declared-effect follow-ups: a friend's lendable cards are read from every world actor on the same side (not just figures on the scene), so a big roster lengthens the list — a scene filter would trim it; the solo dialog can't relay a friend's cost to its owner's client; Dodge shifts (True Shadow, Pinpoint Accuracy) have no layer; surge grants on a use are editable only in `docs/class-cards.json`; strain beyond endurance is refused rather than converted to damage; imperial threat costs are noted, not charged.
- Imperial follow-ups: attachment restrictions ("Trooper only", "Droid or Vehicle only") are printed on the card, not enforced on drop; class-wide passives with a printed condition (Superior Augments' "group with 1+ attachments", Oppression's "hero with 2+ strain") stay text; the combat window reads the Imperial deck for villains only — allies acting as Imperial figures would need an affiliation check instead of the actor type.
- Compendium generator from the dataset JSON + `docs/class-cards.json` transcriptions; art referenced locally, not committed.

**Item QoL**
- Optional: one-mod-per-subtype validation (no double barrels) — table-rules toggle
- Reorder follow-up: cross-column drags (e.g. a mod onto a weapon row) still fall through to the sheet's drop path; a mod dropped on a weapon row could attach it instead.

**Condition follow-ups**
- Stunned's movement restriction (no voluntary movement) is not enforced — the system doesn't track movement points. Card text only.
- Bleeding's "whenever you perform an action" is manual (the Suffer button on the sheet/portal tray); there is no action counter to hook.
- Conditions on the *defender* of a solo, targeted attack are read at roll time only (targeted attacks normally go to the combat window anyway).
- Built-in condition rules can't be edited in the settings form; if a table rule differs (e.g. a house-ruled Hidden), the workaround is a custom condition with a different id.
- Hidden's -2 Accuracy is applied to the attacker's displayed total; range is still adjudicated by eye.

**Known issues**
- `npm run survey` still shells out to `python3` (`class-card-survey.py`, `card-survey.py`), which isn't installed on the Windows dev box; port to Node like `class-card-uses.mjs` if the survey markdown ever needs regenerating there.
- `lang/en.json` is expanded into nested objects by Foundry: a key can never also be a prefix of another key ("X.Y" and "X.Y.z" together reject the whole file, silently, and every label renders raw). `npm run check:lang` (`scripts/build/check-lang.mjs`) catches it; run it after editing the language file.
- Agenda cards: `influenceCost`, `agendaType`, `missionEffect` are in the data model and localized but the shared classcard sheet only exposes its extra fields for `type === "classcard"`; agenda and imperial class cards get name + art + state only.
- Actor sheet special-ability change scrape (`_onSpecialAbilityChange`) writes only name/description, dropping `surgeCost`; it survives only through the edit-mode-exit save path.
- Player Area drop zones show `.can-drop` for owners, but `_onPortalDrop` is GM-only — cosmetic/permission mismatch.
- Item sheets are the only surfaces that do not autosave (explicit Save button); worth a hint in the sheet toolbar or a manual note.
- Portal defense dice over-draw if a GM sets a raw defense attribute above 9 (`buildDefensePool` clamps at 9, the raw render does not).

## Done (this cycle)
- Player Guide + GM Guide: `docs/PLAYER_GUIDE.md` (11 sections) and `docs/GM_GUIDE.md` (16 sections) written against the current code; `scripts/build/md.mjs` (markdown → HTML, H2 = journal page) and a `guides` JournalEntry pack in `build-packs.mjs`, registered in `system.json`; `MANUAL_OUTLINE.md` marked historical.
- Cross-figure declared effects: `scope` (self / friendly / any) on `cardUseList`; `teamOf` / `friendlyFiguresOf` / `friendlyCardUsesFor` / `cardUseOwner` in `common.js`; combat window merges friends' uses into each side's `cardUses` with `ownerActorId`, pays/refunds on the owner, and lets the owner's user declare (`canDeclareUse`); solo dialog offers friends' cards the client may pay for; owner prefix on buttons and surge sources; card sheet **For** field; 13 scoped entries in `class-card-uses.mjs` (Called Shot, Wookiee Loyalty, Get Down, Coordinated Attack, Dig In, Professional Aide ×2, Adrenal Vapor, Stay Behind Me, Scouts Guidance, Ringleader, Targeting Sensors).
- Item QoL batch: drag-to-reorder within columns (`_bindReorder` in the actor sheet, `SortingHelpers.performIntegerSort` on `sort`; every column sorts by `sort` first); "Give to…" (`scripts/item-trade.js`: `promptGive` / `requestGive` / `transferItem`, GM socket relay `giveItem`, mods follow weapons, armor unequips); weapon `exhaustAbilities` editor on the weapon sheet (`_collectExhaustRowsUpdate`) and Use buttons in the hero sheet's weapon details (`useExhaustAbility`); optional XP deduction on class-card drop (`deductClassCardXp` setting, `settleClassCardPurchase` / `chargeClassCardPurchase` on the sheet and the Player Area drop); Companion + Imperial portals migrated onto `item-cards.js` `bindCardPreviews`.
- Imperial attachments + class-wide cards: `ImperialclasscardData` gained `attachment` / `imperialClass` / `classXp` (generator sets them; `imperialCardPatch` lifts flags + "Attachment." text on ready and on create); `isImperialFigure` / `imperialClassWideCards` / `resolveUseItem` in `common.js` — `equipmentSourcesFor` and `cardUsesFor` read owned imperial cards (attachments) plus the world deck's non-attachment cards for villains, `payCardUse` / `refundCardUse` resolve world items and charge / refund `cost.threat` through `adjustCampaignThreat`; villain sheet Attachments panel (`attachments` context, drop via core); `refreshImperialFigures` resets villains when the world deck changes; resource-shift hooks cover imperial cards; imperial card sheet fields; `class-card-uses.mjs` carries threat costs for Versatile Attack / Exacting Strike / Executioner.
- Class-card stage 3 (declared card effects): `cardUseList()` in `common.js` (`when / note / choice / cost{exhaust,strain,deplete} / modifier / surgeAbilities`) on `ClasscardData.use` and `ImperialclasscardData.use`; `cardUsesFor` / `cardUseAvailability` / `payCardUse` / `refundCardUse` / `modifierEffectHTML` / `cardUseCostLabels`; combat window gained a `cardBonus` layer per side, `cardPierce`, `cardUses` with receipts, `useCard` / `unuseCard` intents, cross-side application (`applyUseModifier`, including onto an already-rolled result), refund on cancel, surge grants merged into the result card; solo dialog gained `cardUses` / `declaredUses`, a Class Cards fieldset with toggles, dice in the pool at declare time, payment at roll; class-card sheet view list + row editor (`_collectCardUseUpdate` DOM scrape, `addUse` / `removeUse`); modifier editor partial takes `attr="data-field"` for scraped rows; `scripts/build/class-card-uses.mjs` derives the `use` lists from the survey models with an override table, 78 cards / 96 entries; `build-packs.mjs` emits them; modifier dice are signed (Ferocity swaps a green for a red).
- Class-card stage 2 (shared modifier + passive layer): `modifierField()` / `emptyModifier` / `addModifier` / `modifierChips` in `common.js`; `ArmorData.modifier` replaces `bonusHealth/Block/Evade` (folded in `migrateData`, persisted once by `migrateArmorModifiers` in `swia.js`); `ClasscardData.passive` and `ImperialclasscardData.passive`; `equipmentSourcesFor` / `equipmentEffectsFor` / `equipmentRollNotesFor` replace `armorEffectsFor`; actor derived data extends to `endurance.max` and `speed` (`baseMax` / `equipmentBonus`, `speedBase` / `speedBonus`) on both hero sides; `actor-actions.js` resource-shift hooks cover armor + class cards and endurance as well as health; `buildAttackPool` / `buildDefensePool` add equipment dice, `attackKeywordsFor` adds equipment Pierce, `equipmentAttackBonusFor` / `equipmentDefenseBonusFor` seed the combat window's gear layer on both sides and the solo card; equipment notes join the conditions line; shared `templates/items/parts/modifier-editor.hbs` on the armor and class-card sheets; chips on hero-sheet armor and class-card rows, portal armor cards, item chat cards; `docs/class-cards.json` gained `passive` blocks on 23 hero + 8 imperial cards and `build-packs.mjs` emits `modifier` / `passive`.
- Class-card stage 1 (equipment tag): `heroClass` / `classXp` on weapon, weaponmod, armor and gear (`classDeckFields()` in `items.js`); XP / Starter badges on Class Cards rows (sorted by XP) and on tagged weapon / armor / accessory rows in their own columns; class-deck chip + edit row on the four equipment sheets; `build-packs.mjs` writes the tag into `system` via `tagClassDeck`; `migrateClassDeckFlags` in `swia.js` lifts the 0.1.8 pack flags into the schema once per world (`classDeckMigration` setting) and a `preCreateItem` hook does the same for items imported later from the older packs.
- Compendium generator: `scripts/build/build-packs.mjs` reads the transcribed card JSON and writes `packs/_source/<pack>/*.json` (Foundry CLI source format, deterministic ids, compendium folders) then compiles to LevelDB with `@foundryvtt/foundryvtt-cli`. Six packs registered in `system.json` under one "Imperial Assault" pack folder. Notation → inline icon conversion shared with the sheets' `.swia-glyph`. Class-deck equipment parsed from text into weapon/armor/mod items; hero starters embedded on the hero actors; deployment keyword lines and named abilities become special abilities; surge lines become structured weapon surges or actor surge entries. Art referenced from `assets/cards/` (gitignored). `.gitignore` rewritten: `/docs` and `CHANGELOG.md` are no longer ignored (the card data and the changelog have to ship); `node_modules/` and `assets/cards/` added.
- Player Area fix: the portal's `.identity img` portrait rule (68px, circular) was also hitting the power-token and condition chip icons nested inside `.identity`, blowing them up to portrait size and stacking them. Portrait rule scoped to `.identity > img`; chip icons pinned to 18px inside the portal.
- Sheet layout pass 2: compact weapon rows (one line + expandable details block for surge text, printed abilities and mods; `_expandedItems` on the sheet instance; `toggleItemDetails` flips `hidden` in place with no re-render); stat glyphs via a `swiaGlyph` Handlebars helper backed by `STAT_GLYPHS` / `statGlyphHTML` / `dieSwatchHTML` in `common.js`, applied to the combat window (bonus-row labels, result totals, declared-token line), chat cards, the roll dialog token rows and condition notes; the redundant held-tokens line removed from the combat window; dormant `toggleInventoryPanel` code and CSS deleted. Two-column grid deferred until compact rows are seen on a kitted hero.
- Sheet layout pass 1: the sheet now scrolls (core's window-content clips overflow; the form part is the scroll container and is listed as `scrollable` so position survives re-renders). In display mode the stats block (health/endurance/speed/XP, dice row, Weapon Attack) is sticky at the top while name, abilities and inventory scroll beneath it. Hero abilities sit behind a `<details>` disclosure, collapsed by default with a count badge; open/closed is remembered while the sheet is open. Edit mode is unchanged (its own collapsible sections).
- Token-art fix: `HeroData` gained `healthyTokenImage`; `setWoundedState` stashes the current prototype token art there on every healthy → wounded transition (before overwriting it with the wounded art), and `getHealthyTokenSrc` reads the stash while wounded / the prototype while healthy, so custom token art set through Foundry's token config survives the wound/heal cycle. Heroes wounded before the field existed still heal to the portrait. Smoke-test: set token art distinct from the portrait, wound, heal, confirm the token art is back.
- Conditions pass: new `scripts/conditions.js` owns the status-effect list (conditions first, then power tokens) and a registry of condition records with rules — pool dice when attacking/testing, signed result shifts when attacking (damage/surge/accuracy) or defending (block/evade, and a shift on the attacker's accuracy), discard triggers (after attack, after test, end of activation, spend an action), cannot-attack, and strain-per-action. Built-ins: Focused, Hidden, Bleeding, Stunned, Weakened with their printed rules; the old Blind/Scanned/Recon/Wanted icon-only statuses were dropped from the built-ins (their PNGs stay in `icons/` so a GM can recreate any of them as a custom marker). Custom conditions live in the `customConditions` world setting, edited through Settings → Configure Conditions (`SWIAConditionsConfig`, `templates/settings/conditions-config.hbs`); they merge into `CONFIG.statusEffects` and the pipeline treats them like built-ins. Dice pipeline: the solo dialog and combat window both add condition dice to the pool and fold shifts into a `conditionBonus` layer next to manual/armor/token (breakdown tooltip updated); totals floor at 0 so a -1 Evade can't go negative; the dialog, window and cards show a Conditions line. Stunned gates `SWIARollDialog.open` (warn for players, confirm-override for the GM). After-attack/after-test discards fire when the solo card posts and when the combat window applies damage (Cancel keeps them). Sheet header and Player Area portal share a condition tray partial (`templates/actors/parts/condition-tray.hbs`) with Discard, Spend Action (confirm), Suffer (Bleeding: strain, then damage at max strain), End Activation, and an add picker; helpers in `actor-actions.js`. Flipping the activation token to "activated" also discards end-of-activation conditions (`updateActor` hook, fires once on the acting client).
- Solo-roll parity pass: solo chat cards now carry per-die ↺ rerolls (unlimited until a surge spend locks them; each reroll posts its own die to chat) and spent surge abilities flip to an Undo button that refunds the surge, reverses its effects, readies an exhaust-to-use source card, and unlocks rerolls. The roll dialog gained a Power Tokens fieldset (Damage/Surge on attacks, Block/Evade on defense, none on attribute tests; Any tokens convert per row) — tokens are consumed only when Roll is clicked, so closing the dialog spends nothing; the card shows the Declared tokens line. The combat window and the solo cards now share one set of result-state helpers in `roll-dialog.js` (`replaceFace`, `recomputeFaceTotals`, `applySurgeToState`, `revertSurgeOnState`, `exhaustSurgeSource`, `readySurgeSource`, `rollReplacementDie`, `consumeDeclaredTokens`), so the two surfaces cannot drift. Solo-card actions use one `cardAction` socket relay (author-or-GM, player → active GM; the legacy `spendSurge` wire shape is still accepted). Players relay whenever a GM is online rather than try-then-relay, since a reroll is not idempotent. Combat summary cards are flagged `readOnly` so they never offer reroll/undo. Card actor resolution is token-aware (`ChatMessage.getSpeakerActor`) so unlinked figures exhaust/ready their own items.
- Power tokens declared before the roll: attacker (Damage/Surge) during setup, defender (Block/Evade) until its defense roll — the same window as the manual steppers. Combat state keeps bonuses by source (manual / armor / token) so each undoes on its own; a declared token's effect data is stashed so Undo restores it exactly, and Cancel refunds every declared token. Any tokens convert at spend time. Attacker-side tokens were previously unread. Spending no longer locks rerolls. Combat sides now resolve through the token uuid, so unlinked figures read/spend their own tokens instead of the base actor's.
- Power-token trays on the hero sheet header and portal cards, with GM +/- to grant or remove tokens (replaces hunting through the token HUD). Shared helpers in `actor-actions.js`.
- Map token badge (`scripts/token-badge.js`): power tokens are hidden from the stock status strip via an Actor subclass and drawn as one icon + count per type held, bottom-left of the token.
- Armor effects: armor no longer models defense dice (it never did on the cards). `ArmorData` carries `bonusHealth` / `bonusBlock` / `bonusEvade`; `armorEffectsFor()` in `common.js` is the single chokepoint. First derived-data layer in the system: `prepareDerivedData` adds equipped armor Health onto `health.max` (both hero sides), with `baseMax`/`armorBonus` kept for display and the hero sheet's max input reading `_source`. Item hooks in `actor-actions.js` shift current health by the same delta on equip/unequip/edit/add/remove (damage-preserving). Combat window seeds the defender's Block/Evade bonus row from armor (still adjustable); solo defense cards apply it too. Portal + hero sheet show Health/Block/Evade chips instead of gold-ringed dice.
- Armor sheet layout pass: rebuilt on the weapon card frame (title strip with state pill + cost chip, framed art with a DEFENSE badge, subtitle/traits row, compact meta rows) and gained the fields the printed cards actually use — `traits`, `bonusHealth`, printed `abilities` rows, (surge editor added then removed — defenders don't spend surges). Ability rows on both weapon and armor now save through a DOM scrape (`_collectAbilityRowsUpdate`) instead of indexed dot-paths, which had been re-defaulting `prefix` on every save. Generated chat text cards now show printed ability lines and bonus health.
- Card-art borrows: hover card preview on hero-sheet and portal item rows, 💬 send-to-chat on every row posting the scan (or a generated text card for items without one); new `scripts/item-cards.js` shared by both surfaces; `sanitizeRichHTML` added for scrubbing broadcast HTML
- Player Area party overview: unspent-XP headline (GM-editable, shares `system.xp` with the Campaign Tracker), clickable state pills, health/endurance steppers, per-figure Ready All, Armor column with equip toggles, mod chips on weapon cards, armor dice gold-ringed in DEFENSE, companions nested under their owner
- New `scripts/actor-actions.js`: shared mutations (wound/defeat, stat + XP steppers, armor equip, Ready All, token sync) used by both the actor sheet and the portal so the two surfaces cannot drift
- Confirm dialog on the Healthy → Wounded transition (both surfaces)
- Weapon pool substitution: `poolAttribute` on weapons draws the attack pool from Strength/Insight/Tech (wounded-aware); hero sheet shows the live substituted dice with a note, item sheet shows ? pips
- Pass 2: `exhaustToUse` surge flag, auto-exhaust with undo, depleted gating, Ready All
- Pass 1: armor section, nested mods with attach/detach + slot limits, effective dice, surge lines on weapon rows
- Combat window: reopen on attack when combat already active (no more stranded state)
- Combat window: auto-opens for all connected users (spectators view-only)
- Unlimited dice rerolls until a spend locks them; surge spend undo
- Untargeted attacks open a pick-a-target prompt (sets target reticle, solo-roll fallback)
- Hero sheet display pass: health/endurance steppers, gold Weapon Attack button, Trait label, state pills (Healthy/Wounded/Defeated), quieter borders, aligned stat rows, empty-state cleanup, black-die visibility
- Hero sheet edit pass: compact chip dice editors, Healthy|Wounded stat tabs, custom-slot add tile, side-by-side selects, Token Setup disclosure, stats-only default expansion
